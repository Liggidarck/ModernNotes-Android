<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <exclude domain="database" path="link.db"/>
    <exclude domain="database" path="notes.db"/>
    <exclude domain="database" path="passwords.db"/>
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude domain="sharedpref" path="top.xml"/>
</full-backup-content>
